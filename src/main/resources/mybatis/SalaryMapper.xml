<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.when.mapper.SalaryMapper">
    <select id="getSalary" resultType="SalaryVo">
        SELECT * FROM member_tb;
    </select>

    <insert id="signup">
        INSERT INTO member_tb(userid, usernickname, userpw, useremail)
        VALUES (#{userid}, #{usernickname}, #{userpw}, #{useremail})
    </insert>

    <select id="useridCheck" resultType="Integer">
        SELECT count(*)
        FROM   member_tb
        WHERE  userid = #{userid}
    </select>

    <select id="usernicknameCheck" resultType="Integer">
        SELECT count(*)
        FROM   member_tb
        WHERE  usernickname = #{usernickname}
    </select>

    <select id="useremailCheck" resultType="Integer">
        SELECT count(*)
        FROM   member_tb
        WHERE  useremail = #{useremail}
    </select>
</mapper>